# Photo Watermarker

A simple Python desktop application using Tkinter that lets a user upload a photo and automatically adds a watermark.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate  # On Windows: .venv\Scripts\activate
pip install -r requirements.txt
python -m app.main